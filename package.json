{
  "scripts": {
    "build": "cd waiter-app && npm install && npm run build",
    "start": "cd waiter-app && npm start"
  },
  "dependencies": {
    "node-fetch": "^2.7.0",
    "puppeteer": "^24.33.0",
    "react-easy-crop": "^5.5.6"
  }
}
